# Interactive Resume & Portfolio - Dominik Benger

## Project Overview
Interactive, animated web app replacing static PDF resumes with dynamic data visualizations, filterable career timelines, and portfolio showcase. Deployed at dbenger.com via Vercel.

## Startup Ritual
**At the start of every new session, before taking any action, you MUST:**

1. **Read project status files** (in parallel):
   - `docs/STATUS.md` — current project state
   - `docs/tasks.md` — task backlog and priorities

2. **Check git state** (in parallel):
   - `git status` — uncommitted changes, staged files, current branch
   - `git log --oneline -10` — recent commit history
   - `git diff --stat` — summary of working tree changes

3. **Summarize current status to the user**, including:
   - Current branch and any uncommitted work
   - What was done in the last few commits
   - Any open tasks or next steps from `docs/tasks.md`
   - Anything that looks like it needs attention (e.g., uncommitted changes, failing builds)

4. **Wait for the user's instructions** before making any changes.

Do NOT skip this ritual. Do NOT start modifying files until you have read the above and presented the summary. This ensures continuity across sessions and prevents accidentally overwriting in-progress work.

## Tech Stack
- **Framework:** Next.js 15 (App Router, TypeScript) — used for API routes + static HTML serving
- **Frontend:** Self-contained HTML (`public/site.html`) with CDN Tailwind CSS, vanilla JS, hash-based SPA routing
- **AI Features:** 4 server-side API routes proxying to Gemini 3 Flash Preview (Google AI Studio)
- **Sanitization:** DOMPurify (CDN) for AI HTML responses
- **Contact Form:** Formspree (in HTML)
- **Fonts:** Plus Jakarta Sans (Google Fonts CDN)

## Project Structure
```
src/
├── app/
│   ├── route.ts           # GET / — serves public/site.html (force-static)
│   └── api/ai/            # Server-side Gemini proxy routes
│       ├── knowledge.ts               # Shared professional profile for all AI routes
│       ├── solution-matcher/route.ts   # POST — 3-step action plan
│       ├── experience-qa/route.ts      # POST — experience Q&A
│       ├── outreach-drafter/route.ts   # POST — email/LinkedIn drafts
│       └── agenda-builder/route.ts     # POST — call agenda generator
public/
├── site.html              # Full site (SPA, CDN Tailwind, hash routing)
├── icon.svg               # Favicon
├── og-image.png           # Open Graph image (1200×630)
├── resume/                # PDF resume for download
└── logos/                 # Company logo assets (6 PNGs)
docs/
├── index.html             # Source design (single source of truth)
├── PRD.md                 # Product Requirements Document
├── Spec.md                # Technical specification
├── Plan.md                # Implementation plan (phases & tasks)
├── STATUS.md              # High-level project status
├── tasks.md               # Detailed task tracker
├── Offering.md            # Service offerings content source
├── Dominik_Benger_Resume_4Page.md  # Source resume content
└── resume-file/           # Original PDF resume source
```

## Commands
- `npm run dev` — Start dev server (Turbopack)
- `npm run build` — Production build
- `npm run lint` — ESLint

## Design System
- **Aesthetic:** Modern teal-accented with dark sections
- **Font:** Plus Jakarta Sans (Google Fonts CDN, weights 300–700)
- **Brand palette:** Teal scale — brand-50 `#f0fdfa` through brand-950 `#042f2e`, primary brand-500 `#14b8a6`
- **Backgrounds:** White, zinc-50, zinc-900/950 (dark hero/footer sections)
- **Animations:** CSS scroll-triggered via IntersectionObserver + `data-animate` attributes (0.35s ease-out-quart, 0.04s delay step — max fade 0.59s end-to-end), hero canvas skill-word constellation (v2: Poisson-disk layout, Lissajous oscillation, hover proximity highlight, ambient connection lines)
- **SPA routing:** Hash-based (#home, #experience, #projects, #collaboration, #contact, #ebook)

## Architecture Decisions
- **Static HTML + API routes**: Self-contained HTML (`public/site.html`) served via Next.js route handler. Next.js used only for API routes and Vercel deployment.
- **SPA with hash routing**: 5 page views (#home, #experience, #collaboration, #contact, #ebook) in single HTML file, JS-based router
- **Server-side AI proxy**: 4 API routes under `/api/ai/` proxy to Gemini 3 Flash Preview — keeps `GEMINI_API_KEY` server-side
- **CDN dependencies**: Tailwind CSS, DOMPurify, Google Fonts all loaded via CDN in HTML
- **No React on frontend**: All interactivity is vanilla JS (scroll animations, experience filters, AI feature forms, canvas particle animation)
- **Design source of truth**: `docs/index.html` — any design changes should be made there first, then copied to `public/site.html`

## Content
- **Owner:** Dominik Benger
- **Domain:** dbenger.com
- **LinkedIn:** https://www.linkedin.com/in/dombenger/
- **Email:** domi@dbenger.com
- **Resume data:** `docs/Dominik_Benger_Resume_4Page.md`
- **PDF download:** `docs/resume-file/Dominik Benger - Resume [V3].pdf`

## Z-Index Stacking
All stacking context is managed in `public/site.html` via inline Tailwind classes. The sticky nav bar is at the top, experience filter pills are sticky below it.

## Pitfalls
- **PDF path must match** — download button points to `/resume/Dominik_Benger_Resume.pdf`; source file in `docs/resume-file/` has spaces but was copied with underscores to `public/resume/`.
- **`.gitignore` has `*.png`** — Exceptions: `!public/logos/*.png`, `!public/og-image.png`.
- **`GEMINI_API_KEY` env var** — Required for all 4 AI features. Set in `.env.local` (gitignored) for dev, in Vercel project settings for prod. If missing or placeholder, API routes return graceful error.
- **Design source of truth** — `docs/index.html` is the master design. Changes should be made there first, then reflected in `public/site.html` (with image path fixes and API route updates).
- **Image paths in site.html** — Must use absolute paths (`/logos/google.png` not `logos/google.png` or `../public/logos/google.png`). Fixed during copy from `docs/index.html`.
- **AI responses use innerHTML** — Solution Matcher and Agenda Builder return HTML from Gemini. Sanitized via DOMPurify with allowlisted tags (`p`, `ul`, `li`, `strong`, `ol`, `em`).
- **route.ts at app root** — `src/app/route.ts` serves static HTML. Cannot coexist with `page.tsx` in same directory. No `layout.tsx` needed (Route Handlers don't use layouts).
- **Gemini API field names** — API routes use `system_instruction` (snake_case) for the Gemini REST API v1beta endpoint.
- **Gemini thinking tokens** — Gemini 3 Flash Preview uses thinking tokens that count against `maxOutputTokens`. Must set `thinkingConfig: { thinkingBudget: 128 }` (not 0) and `maxOutputTokens: 1024`. At `thinkingBudget: 0` with temperature > 0.3, the model outputs garbage text.
- **AI knowledge base** — All 4 AI routes import `src/app/api/ai/knowledge.ts` which contains Dominik's full professional profile. Update this file when resume content changes.
- **AI plain text outputs** — Experience Q&A and Outreach Drafter must include "Output PLAIN TEXT only" in system prompts to prevent markdown artifacts (`**bold**`) rendering as literal text.
- **Hero canvas DPR** — Canvas must scale `canvas.width/height = rect.width/height * dpr` and call `ctx.setTransform(dpr,0,0,dpr,0,0)` — otherwise text is fuzzy on retina. Cap DPR at 2 for perf.
- **Hero canvas sync** — FEATURE 5 block (`public/site.html` ~3026–3360, `docs/index.html` ~3055–3389) must stay byte-identical between both files. Use `diff` to verify after edits.
- **Hero canvas motion speed** — Lissajous frequency ~0.001–0.002 rad/ms (period 5–8s) reads as "alive"; `0.0004` (period ~15s) reads as static.
- **Scroll-entrance timing** — `[data-animate]` uses `opacity/transform 0.35s cubic-bezier(0.22,1,0.36,1)` (ease-out-quart) with delay step `0.04s` per `data-delay` level. End-to-end fade (delay + duration) caps at ~0.59s. Longer values (0.6s + 0.1s step = 1.2s total with ease-out-quint) read as "slow motion" trailing the scroll.
- **CSS transition shorthand cascade** — `[data-animate]` sets its transition via the `transition` shorthand, but Tailwind CDN utility classes like `transition-colors` (loaded later in the cascade) override it on elements that have both. Those elements snap instead of fade. Pre-existing; not worth fixing since it affects only small above-the-fold elements (nav pills, etc.).
- **content-visibility on below-fold sections** — `.page-view > section:nth-of-type(n+2) { content-visibility: auto; contain-intrinsic-size: auto 1200px; }` defers paint/layout of non-hero sections. Works correctly with the `data-animate` IntersectionObserver (`rootMargin: '0px 0px -40px 0px'`) because the browser materializes `cv:auto` content before viewport intersection.
- **Compositing-cost traps** — `mix-blend-multiply` on blurred elements blocks GPU layer promotion → forces CPU paint each frame; `backdrop-filter: blur()` on a fixed strip forces per-scroll-frame recomposition; `animate-pulse` on large blurred layers = continuous repaint. All three are invisible on M-series GPUs but bite mid-range devices.

## Accessibility & Responsive
- All accessibility and responsive behavior is handled in `public/site.html` via Tailwind responsive classes and HTML attributes
- Scroll animations use IntersectionObserver with `data-animate` attributes
- Experience filter uses `data-tags` on cards for tag-based filtering
- Mobile-first responsive design via Tailwind breakpoints (sm, md, lg, xl)

## Performance Profile
- Build: 0 warnings, `/` is static (force-static), 4 API routes are dynamic
- `/` = 101 kB first load JS (gzipped) — static HTML response
- `site.html` = ~250 kB (self-contained with inline CSS/JS + CDN Tailwind)
- `next.config.ts`: `reactStrictMode: true`, `poweredByHeader: false`

## Repository
- **GitHub:** https://github.com/Ninety2UA/web-app-resume
- **Commits:** `fb6a036` (Phases 0–7), `34e5062` (Phase 8 + launch prep), `53bdd97` (post-launch UI fixes), `47ba309` (Skills & Tech Stack merge), `18a2ea5` (Collaboration page), `b5e26ea` (UI rework: chart, timeline, nav), `2d65169` (logos, full resume content, nav fix, RIT logo, README), `6863c84` (comprehensive README + CLAUDE.md sync), `e8f9cae` (doc sync), `78bb8f6` (deployment complete), `ec931fc` (RIT logo update), `1f47fd8` (mobile layout fixes), `648994d` (Google intern date fix), `d083605` (resume PDF V3 update), `fb4ece0` (mobile nav + chart spacing), `9ce59d3` (contact anchor scroll fix), `e2d7434` (experience anchor scroll fix), `4095704` (ebook page), `6cfa572` (AI chatbot + ebook Gemini/plugin update), `a52b4a8` (README update), `27e65c1` (chatbot mobile auto-open fix), `0366d57` (ebook summary + hero button visibility), `8d7629c` (full redesign: static HTML SPA + AI knowledge base + Gemini thinking fix), `d9bb422` (OG image + meta description update), `5befda8` (mobile nav, status badge, hero particles fix), `1571bc9` (Collaboration in mobile nav + ebook hero pill), `6884d22` (mobile nav overflow fix), `cf3137f` (README update), `0018dcc` (ebook pill to nav bar desktop), `fc5a793` (remove Ebook nav link), `0f6743d` (Android mobile nav overflow fix), `45f49de` (README update), `7fd89f5` (resume PDF update + Career Break → Professional Development), `24300d6` (Projects page with 6 portfolio entries), `db6a3d8` (Resume V4 content update), `fda3203` (restore original V4 PDF + iterative copy refinements), `9fdc90e` (OG image + README for Projects page + Resume V4), `396c47a` (hero canvas rework: interactive skill constellation), `93a9c5c` (Phase 28 doc sync), `2d553ad` (Phase 29 scroll perf polish), `43ec3df` (Phase 29 doc sync), `25fc0f3` (cycling outreach planning docs), `33e66f9` (AGENTS.md + gitignore agent tooling), `a3bb5d2` (Langdock prototype depth upgrade plan)
- **Branch:** `main`

## Project Documentation
- Read `docs/STATUS.md` for current progress and state
- Read `docs/tasks.md` for the task backlog and priorities
- Read `docs/plan.md` for the overall project plan
- Read `docs/spec.md` for project specifications and requirements

Always read these files before starting any work.

## Session Continuity
- **Sibling project (2026-07-27, Langdock site — home fit-card copy rework, DEPLOYED)** — A screenshot-driven `/ce-work` session in `~/projects/langdock-application` fixing two things Dominik flagged on the live home page. (1) **The third adoption stat**: "Multi-billion USD / in client advertising revenue supported by that platform" was the only word-valued figure in a column of numerals and wrapped to two lines at the 44px display size, and "that platform" was a dangling back-reference that only parsed off the stat above it. The masked figure now carries **two forms in the data module** — `platformRevenueSupported` ("Multi-billion USD") stays whole for prose because the concierge knowledge base reads it, while new `platformRevenueFigure` ("Multi-billion") renders in the stat tiles with "USD" moved into the label: "USD in client advertising revenue covered by that same BI platform". "Covered" stays the load-bearing verb (a BI platform covers revenue, it never processes it). (2) **The model-agnostic card** claimed model-agnostic construction while naming only two of the three systems that make it true: **Claude Code Blueprint** now leads with its own paragraph, Agent Triforge follows, and the practice loses its proper noun for "my own operating layer" plus the depth it was missing (four pillars over a three-tier index, delegation and orchestration, daily loops for briefing/wrap/compounding). Also fixed a **real typographic bug**: Triforge's line interpolated `triforge.facts[0]`, putting a capitalised "Six-CLI" mid-sentence. All five decisions were Dominik's, taken through four `AskUserQuestion` menus with two clarification rounds. **Name scope: home (fit card + PathCards) and /about only — `/systems` keeps naming Ninety2**, which is the deliberate split (plain language at the front door, the named system where the deep-dive introduces it). Six files, +30/−13. Langdock repo `main` at `746499d`, pushed, `npx vercel --prod` (target production, Ready), live-verified on all three pages. **735 Vitest, lint 0, build clean.** Measured in Chrome 1440 and **WebKit at a true 390px**: all three stat values now sit on one line (the prior shape broke as "Multi-billion" / "USD"), 0 horizontal overflow, 0 console errors, and all three cards measure exactly 487px because CSS grid stretches the row — so paragraph count never unbalanced anything. **No web-app-resume source changes** (this note only). Lessons in auto-memory (`langdock-application-site.md`).
- **Sibling project (2026-07-27, cycling site — FINAL briefing run, Tour archive closed, DEPLOYED)** — Ran the cycling briefing ritual one last time in `~/projects/cycling-application`. The 2026 Tour finished 2026-07-26 and two mornings had been missed, so this was a close-out rather than a morning publish. Graded stage 19 (Gap to Alpe d'Huez): Pogačar attacked out of the yellow-jersey group ~12 km out, caught the break's remnants inside 2 km and went clear again at ~900 m, climbing the Alpe in 35:26 to become the second rider after Geraint Thomas in 2018 to win there in yellow → `scenarioOccurred: "GC attack"`, the packet's top call (47%), CORRECT, `topFavoriteWon: true`. Backfilled stages 20 and 21 as RECONSTRUCTED graded packets: stage 20 (queen stage, Le Bourg d'Oisans to Alpe d'Huez over Croix de Fer, Télégraphe, Galibier, Sarenne) went to Richard Carapaz from the break after Sepp Kuss crashed twice on the wet Sarenne descent, sealing the mountains classification → "Breakaway", top call (60%), CORRECT; stage 21 was **not the announced stage** — wildfires in the Gironde and Landes (36,000+ ha) pulled the finale's police cover, so ASO rebuilt it at ~24h notice, dropping the Thoiry start and running 88.7 km start-and-finish on the Champs-Élysées, where Mathieu van der Poel beat teammate Philipsen and Pedersen → "Full sprint" against a top call of "Reduced sprint", an honest miss. Default bumped to `2026-21` (the live→reconstructed bump variant: full default-render rewrite to reconstructed badge + permanent label + archive strip). **Archive closes at 21 of 21 briefed, 6 live + 15 reconstructed; calibration reads 6 of 6 on the top scenario and 4 of 6 on a top-5 favourite.** Also swept the now-false "the Tour being raced right now" copy from five places (briefing hook bookend, home flagship card, /briefing OG card, concierge knowledge base, Bahrain pitch) plus two README lines, and closed out `docs/briefing-ritual.md` with a status banner and a new section 11. Commits `914286b` + `65c4d1d`, pushed, `npx vercel --prod`, live-verified on prod. **745 Vitest, lint 0, build clean, green on the first run.** One OPEN item for Dominik: the skeleton still carries stage 21's announced Thoiry route (133 km), so the hero reads 133.0 km while the packet describes the 88.7 km actually raced, and the packet's elevation marks and decisive sector are 88.7 km positions plotted on a 133 km axis. The reason on the day was that the committed route geometry is emitted from the announced-route GPX, so relabelling the start without a matching polyline would put a wrong map under a right label; no search for a replacement polyline was run before deciding. A later check found the published stage 21 GPX (cyclingstage, intervals.icu) is the ORIGINAL Thoiry route and surfaced no GPX of the rebuilt circuit, which is a failed search rather than proof none exists. The first write-up of this asserted the stronger claim without checking and put it in `docs/briefing-ritual.md`; corrected there in `dad68b8`. **No web-app-resume source changes** (this note only). Lessons — including that PCS is scrapeable only via `firecrawl scrape --format html` and its rider/team table columns drift, so every GC gap must be reconciled by arithmetic against the prior committed packet — in auto-memory (`cycling-application-site.md`).
- **Sibling project (2026-07-27, Langdock site — prototype depth upgrade BUILT + DEPLOYED)** — A `/ce-work` session executed the 2026-07-26 plan end to end: the `/prototypes` console now serves **21 six-stage workspaces (Brief, Analysis, System, Evidence, Decision, Handoff)** built from a shared kit under `src/components/prototypes/kit/`, with every prototype's data in `src/data/prototypes/scenarios/<slug>.ts` under hardened copy/colour/grammar/shape/wiring gates. "Ninety2, live" removed; **AI Incident and Escalation Room** and **Platform Migration and Consolidation Planner** added as Delivery flagships (registry 21 entries, 4/6/5/4/2, five flagships). The U7 check-in happened mid-session: Dominik approved the grammar, full 21 scope, the Fit Mapper delta-vs-baseline rail, and design-claims for the 90-Day Plan's analysis stage. Foundation + pilot serial; families as five parallel agents with exclusive file ownership (three died at a session usage limit but their work survived on disk — 2 test fixes recovered it). Simplify pass cut ~1,280 duplicated lines; full `ce-code-review` (6 personas + a Codex cross-model peer that timed out at its 600s cap on the 48k-line diff) yielded 9 validated findings, all applied — including two real copy-gate holes (the what-if compute sweep passed step labels instead of indices; the vendor-rule inventory exemption matched any path segment) and four concierge follow-up rewordings. Langdock repo: `main` at `9196f11`, **735 Vitest**, lint 0, build clean, deployed via `npx vercel --prod` and live-verified (21/21 Chrome sweep on prod, WebKit true-390 with no schematic collapse, live concierge answering the new prototypes' follow-ups). Residuals in that repo's `docs/residual-review-findings/feat-prototype-depth-prototypes.md`; verification log in its `docs/browser-smoke.md`. Only the send + copy approval remain Dominik's. **No web-app-resume source changes** (this note only). Lessons in auto-memory (`langdock-application-site.md`).
- **Sibling project (2026-07-26, Langdock site — prototype depth upgrade, PLANNED ONLY, superseded by the 2026-07-27 build above)** — A `/ce-plan` session producing `docs/plans/2026-07-26-001-feat-prototype-depth-upgrade-plan.md` (commit `a3bb5d2`, pushed). No source changes in either repo. The plan takes the `/prototypes` console from 20 thin single-screen prototypes (425–955 LOC each, 2–5 `useState`, no schematics, no visible reasoning) to **21 deep workspaces on one shared six-stage grammar: Brief, Analysis, System, Evidence, Decision, Handoff**. Built from a kit of `Stage` + `StageIndex` + six premium components (analyst thread, schematic canvas, drill-down table, what-if rail, verdict card, provenance drawer), with scenario data split into `src/data/prototypes/scenarios/<slug>.ts`. **"Ninety2, live" is removed** (it answered "what does Dominik run at home" while the other 19 answer "what could Langdock do for an enterprise", and `/systems` now tells that story twice) and replaced by an **AI Incident and Escalation Room** plus a **Platform Migration and Consolidation Planner** — both Delivery flagships, 21 total, partition 4/6/5/4/2, five flagships. Direction chosen by Dominik from a rendered mockup Artifact (https://claude.ai/code/artifact/93df9bcc-933d-40ad-9ea2-2d8606f3d2c7) that inlines the real Instrument Sans from `.next/static/media/`. **Reviewed in three passes, 31 fixes applied** (round 1: six personas inline on Opus 5; round 2: seven dispatched on Fable 5 against the plan AND the target repo). **Execution order is counterintuitive: foundation starts at U2, not U1** (U2 makes the colour gate recursive; U1 creates the first `kit/` file that gate must see), and **U7 is a hard stop** carrying three decisions for Dominik: grammar approval, a paper six-stage mapping for the Fit Mapper and 90-Day Plan (the pair the grammar fits worst), and go/no-go on timing. One open question stands: the send window and wall-clock budget (Q1, non-blocking, resolves at U7). **Nothing is built.** Next session starts in `~/projects/langdock-application` with `/ce-work <plan path>`. Lessons in auto-memory (`langdock-application-site.md`).
- **Sibling project (2026-07-24, Langdock site — Creative Health client case study on /systems, PLANNED + BUILT + DEPLOYED)** — A `/ce-plan` then build session adding a new deep-dive to `/systems` (after the Ninety2 deep-dive, before the ask) telling the story of the **creative-health-demo ("Pip's Peaks")** client project: a Google-ADK creative "trading desk" that scores every ad creative and issues one verdict each (Scale/Sustain/Fix/Retire), built for a mobile-app growth advertiser and forked into a public demo on synthetic data. Direction A (native deep-dive) chosen by Dominik from a **3-direction mockup Artifact** (A deep-dive / B spotlight / C console). Two REAL screenshots captured from the live tool (the decision-map bubble matrix + the diagnosis/action-plan/governed-chat) into `public/images/`; facts in new `src/data/creative-health.ts`; new `CreativeHealthDeepDive.tsx`, wired into `systems/page.tsx`. **Public-safe by construction** — the real client and every internal identifier stay off the page, masked as "Pip's Peaks" and enforced by `creative-health.test.ts` (banned client tokens built from char codes + infra-pattern regexes, mirrors the source project's `deploy/scrub-gate.sh`). Gates green (lint 0, **401 Vitest**, build clean); Chrome 1440/390 + WebKit 390 verified (0 overflow, both images render); MERGED to main (`349bce8`), pushed, `npx vercel --prod` (deploy `langdock-application-kvnb26dus`), live-verified on langdock.dbenger.com/systems. Plan at `docs/plans/2026-07-24-001-feat-creative-health-systems-section-plan.md`. **No web-app-resume source changes** (only the plan doc + this note). Lessons (chrome-devtools CLI drives a SEPARATE Chrome not the user's logged-in browser; `&nearr;` renders literally in SWC JSX, use the lucide `ExternalLink` icon; char-code confidentiality gate; force instant scroll for section captures) in auto-memory (`langdock-application-site.md`).
- **Sibling project (2026-07-24, Langdock site — Safari/iPhone bug fix + mobile nav menu, DEPLOYED)** — A `/ce-polish` + `/ce-work` session fixing what Dominik saw broken on his iPhone (missing pictures, misalignment, missing features). Root cause was TWO things. (1) **Production was serving an UNMERGED Codex branch** `codex/premium-imagery` (3 "premium narrative" images on home/about/systems, `vercel --prod`'d straight to prod but never merged to main) — caught because the live `/images/*.jpg` existed in NO committed state of main, then `git fetch` surfaced the branch. (2) Those images used `next/image fill` inside a `<figure>` sized ONLY by CSS `aspect-ratio`, which **collapses to 0 height in WebKit/Safari** (renders fine in Chrome) — so the pictures were invisible on iPhone. Fix (`8ac9555`): reserve the frame height with an in-flow padding-ratio spacer via a required `ratio` prop (keeps the `object-cover` crop + desktop `items-stretch`); merged the branch (with fix) to main. Then on Dominik's follow-up, added a **mobile hamburger menu** (new `MobileNav.tsx`, `5207106`) so the full DB × Langdock logo + every link (incl. "Why Langdock + Dominik") reach mobile without re-triggering the old 390px nav overflow (tablet/desktop unchanged). Both merged to main (HEAD `5207106`), pushed, deployed (final redeploy `dpl_5H5b6LixtT1mf4H8assxktF1ZxgK`), live-verified in REAL Safari at 390px (images render, full logo + menu open with all 6 links, 0 overflow). Gates green (lint 0, **388 Vitest**, build clean). **KEY LESSON: chrome-devtools / Chrome emulation is Blink and CANNOT reproduce Safari/iOS-only bugs — the collapse was 100% invisible in Chrome even at true 390px; reproduce "broken on iPhone" reports with `playwright-cli --browser=webkit` (install with the wrapper's bundled playwright: `node <@playwright/cli>/node_modules/playwright/cli.js install webkit`).** **No web-app-resume source changes** (docs-only continuity note). Full detail + pitfalls in auto-memory (`langdock-application-site.md`).
- **Sibling project (2026-07-23, Langdock site — ITERATED + REDEPLOYED across ~6 screenshot-driven rounds)** — A long refinement session on the now-live Langdock application site (`~/projects/langdock-application`, HEAD `31f3e34`, live at https://langdock.dbenger.com, still permanent-noindex; ~20 commits over ~6 ff-merge-to-main + `npx vercel --prod` rounds, each gated green: lint 0, **382 Vitest**, build clean, and chrome-devtools/CLI verified at 0 console errors + 0 overflow at true 390px). Highlights: (1) **`/prototypes` console rearchitected through four iterations** — single chat window → Cursor-style left "workbench" sidebar (chosen by the user from a rendered mockup Artifact + AskUserQuestion) → **multi-session chats** (each prototype opens its own chat you can converse about; "New question" opens a concierge chat; the sidebar lists open Chats distinct from the catalog launch menu) → a unified **session pane** (run + inline artifact + chat + composer in one bordered Cursor-style pane). (2) **10 → 20 prototypes**: 10 new premium chart-rich ones built by a 10-agent ultracode Workflow, and the original 10 enhanced by a second 10-agent Workflow. (3) **klar-native home**: new "Why Langdock + Dominik" **About page** + hero CTA; a live inline concierge on the home hero **plus a floating "Ask me anything" launcher on every other page** (shared root `ConciergeProvider`); the klar hero headline "This is not a cover letter. It is *a working application for Langdock*." (blue-accent payoff); a **premium founder-bio intro panel** (Mobbin + /impeccable); the "Full Portfolio at dbenger.com" nav CTA replacing the contact pill; the **Langdock chevron mark** added to the DB × Langdock lockup. (4) **Copy made factual/polite**: removed the Kimi K3 model tag everywhere (model still pinned + disclosed server-side on request), reworked hero/fit/ask copy, dropped "Munich", and swapped the inflated "20+ projects" stat for the documented "50+ colleagues trained". **No web-app-resume source changes** (docs-only continuity note). Full per-round detail + pitfalls (logo.dev is token-gated so used Langdock's own CDN mark; chrome-devtools CLI keeps losing the tab to the user's other windows; multi-session mount-race fixed by gating the seed effect on `!hasDeepLink`; concierge chats retitle by first message so tests scope with `!el.closest("button")`) in auto-memory (`langdock-application-site.md`).
- **Sibling project (2026-07-22/23, Langdock application site — BUILT, REVIEWED, POLISHED, DEPLOYED)** — Executed the plan `docs/plans/2026-07-22-001-feat-langdock-application-site-plan.md` to its Definition of Done in one `/goal` → `/ce-work` session, building the FIFTH and most important application site in the sibling repo `~/projects/langdock-application` (own private repo `Ninety2UA/langdock-application`, Vercel, live at **https://langdock.dbenger.com**, permanent noindex). Single recipient: Judith Dada, Langdock's incoming co-CEO. All 15 units: Langdock design system + Instrument Sans (typeface gate approved by Dominik), the Cursor-grammar prototype console with all 10 authored prototypes (3 flagships + 7 baselines built by a 14-agent Workflow of authors + adversarial verifiers), the Kimi K3 concierge (OpenRouter proxy, honest degrade ladder, injection-guarded), the Agentic Systems page with four hand-drawn Excalidraw figures, the hosted ninety2 walkthrough copy (source byte-untouched), OG card, and the QA docs. Two mandated check-ins passed (typeface + flagships). Then a full `/ce-code-review` (7 local reviewers + a **codex cross-model adversarial lens**; 20 findings, no P0, all validated; 19-item queue applied inline, `4759fb6`) and a chrome-devtools/playwright **5/5 visual pass** (`0960ef1`): reworked the home hero from an empty right half into a two-column layout with a live console glimpse, and fixed a **real 129px mobile-nav horizontal overflow at true 390px** that the earlier browser-smoke had missed (chrome-devtools clamps its window to ~500px — only playwright's true 390 viewport exposed it). Final: 28 commits, 283 Vitest green, lint 0, build clean, deployed and live-verified (0 console errors, 0 mobile overflow, concierge grounded). Only the send + final copy approval remain Dominik's (pre-send-checklist in the langdock repo). **No web-app-resume source changes** (only this repo's plan doc). Full detail + pitfalls in auto-memory (`langdock-application-site.md`).
- **Sibling project (2026-07-23, daily briefing)** — Ran the cycling briefing ritual for stage 18 and deployed. Graded stage 17 (Chambéry to Voiron): Jasper Philipsen (Alpecin-Premier Tech) won the sprint out of the large breakaway that went clear over the opening climbs and was never brought back, ahead of Mauro Schmid and Olav Kooij, so `scenarioOccurred: "Breakaway"` records the packet's top call (46%) as CORRECT and `topFavoriteWon: true` (Philipsen was favourite #2); GC unchanged, green cut to seven points. Published stage 18 LIVE (Voiron to Orcières-Merlette, 185.2 km, 3,900 m, first of three Alpine summit finishes) at ~10:55 CEST, ~1h40 before the 12:35 start (R20 met): Breakaway 60 / GC attack 28 / Reduced sprint 12, favourites Carapaz/Arensman/Simmons/Johannessen/Kuss, dark horse Valentin Paret-Peintre. Bumped `defaultStageId` to 2026-18 — the live-to-live bump is the easy variant (only heading, `185.2 km` basics assertion and the `/Stage 18,/i` chip move; badge assertions untouched), sentinels → 2026-19. Also corrected two stale team names caught by grepping the committed packets: Olav Kooij is Decathlon CMA CGM Team (stage 17 had Team Visma | Lease a Bike) and Thymen Arensman is Netcompany INEOS (stage 15 had INEOS Grenadiers). Only 3 elevationMarks shipped because no source published summit altitudes for three of the five climbs — omitted rather than approximated. Gates green FIRST run (742 Vitest, lint 0, build clean); committed `da6e669`, pushed, `npx vercel --prod`, live-verified (default Stage 18 live badge, stage 17 deep link shows Philipsen/Breakaway). 18 of 21 briefed. Next run 2026-07-24: grade stage 18, publish stage 19 LIVE (Gap to Alpe d'Huez, 127.9 km mountain summit finish). No web-app-resume code changes. Lessons in auto-memory (`cycling-application-site.md`).
- **Sibling project (2026-07-22, daily briefing)** — Ran the cycling briefing ritual after one missed morning (07-21). Backfilled stage 16 (Évian-les-Bains to Thonon-les-Bains ITT, 26.1 km) as a RECONSTRUCTED graded packet with the TT scenario set ("Favourite wins" 58 / "GC shake-up" 27 / "Outsider upset" 15): Evenepoel won in 32:19, 28 seconds over Pogačar (`scenarioOccurred: "Favourite wins"`, `topFavoriteWon: true`), overall gap cut to 4:32; Florian Lipowitz crashed out of 5th on the descent. Published stage 17 LIVE (Chambéry to Voiron, 174.7 km flat) at ~11:20 CEST, ~2h before the start (R20 met): Breakaway 46 / Reduced sprint 30 / Full sprint 18 / GC attack 6, favourites Pedersen/Philipsen/Kooij/Mohorič/van der Poel, dark horse Quinn Simmons. Bumped `defaultStageId` to 2026-17 — default-render test flipped back to the live-badge shape (full assertion rewrite), sentinels → 2026-18, round-trip chip `/Stage 17,/i`. Three parallel research agents cross-corrected (a Voisard/Jegat team transposition in the source GC table caught and verified; white-jersey arithmetic reconciled across agents); house team spellings verified against committed packets (Alpecin-Premier Tech, Netcompany INEOS, NSN Cycling Team — agents' training-data names are stale); missing town altitudes sourced from fr.wikipedia/Wikidata commune elevations; a Racing Post URL was excluded from sources because the betting-lexicon gate walks source URLs too. Gates green FIRST run (741 Vitest, lint 0, build clean); committed `3541227`, pushed, `npx vercel --prod`, live-verified (default Stage 17 live badge, stage 16 deep link Reconstructed, calibration "3 of 3 live"). 17 of 21 briefed. Next run 2026-07-23: grade stage 17, publish stage 18 LIVE (Voiron to Orcières-Merlette, 185.2 km mountain summit finish). No web-app-resume code changes. Lessons in auto-memory (`cycling-application-site.md`).
- **Sibling project (2026-07-19, daily briefing catch-up)** — Evening run (21:40 CEST) of the cycling briefing ritual after two missed mornings (07-18, 07-19), with today's stage already finished — so per runbook section 8 there was no live publish. Graded stage 13 (Mauro Schmid, Team Jayco AlUla, won from the 40-rider break in Belfort → `scenarioOccurred: "Breakaway"`, the 64% top call CORRECT, `topFavoriteWon: false`; Pidcock 3rd at 2s vaulted 10th→4th on GC). Backfilled stages 14 and 15 as RECONSTRUCTED graded packets (asOf = nominal mornings 08:00): stage 14 (Mulhouse to Le Markstein, 155.3 km) — Pogačar soloed off the maiden Col du Haag for his fourth win, "GC attack", favourite won, lead 3:36→4:30; stage 15 (Champagnole to Plateau de Solaison summit finish, 183.9 km) — Evenepoel countered del Toro in the final km, "GC attack", `topFavoriteWon: false` (not on the morning favourites list), on the day Vingegaard crashed out with a broken collarbone and Merlier missed the time cut. Bumped `defaultStageId` to 2026-15: this bump VARIANT flips the default-render test back onto the reconstructed badge + permanent label + archive strip (full assertion rewrite, not a literal swap); sentinel ids → 2026-16; the injected-failure test needed NO move (non-default target, fully mocked). Three parallel research agents cross-corrected each other (a GC team transposition caught twice independently; both agents' "GC after 14" tables matched exactly with bonus-second arithmetic reconciling); normalized their stripped/invented diacritics and team spellings before emitting via a house-style formatter (compact leaf objects, round-trip asserted). Honesty calls: estimate-flagged GC rows replaced with sourced ones; stage 14 weatherSnapshot all-null ("Morning forecast not recovered") + real archive stage-day block. Gates green FIRST run (739 Vitest, lint 0, build clean); committed `13fb34c`, pushed, `npx vercel --prod`, live-verified (default Stage 15 reconstructed, calibration "top scenario called in 3 of 3 live briefings"). 2026-07-20 is a REST DAY — next run 2026-07-21 publishes stage 16 LIVE (Évian-les-Bains to Thonon-les-Bains ITT, 26.1 km), which needs a TT scenario set (not the four road labels), default → 2026-16, default-render pins flip back to the live badge. No web-app-resume code changes. Lessons in auto-memory (`cycling-application-site.md`).
- **Sibling project (2026-07-17, daily briefing)** — Ran the cycling site's daily briefing ritual for stage 13 and deployed, per `~/projects/cycling-application/docs/briefing-ritual.md`. Graded stage 12 (Magny-Cours to Chalon-sur-Saône): Tim Merlier (Soudal Quick-Step) won a chaotic full bunch sprint (a late final-km crash took down Gaviria and Wærenskjold) for his third win of this Tour, so `outcome` records `scenarioOccurred: "Full sprint"` (a correct top call) and `topFavoriteWon: true` (Merlier was favourite #2); clean +10-line hand-inserted edit, no reformat. Researched + published the stage 13 live packet (Dole to Belfort, 205.8 km hilly, the Tour's longest stage; a breakaway day over the Cat-1 Ballon d'Alsace ~30 km out into a wet descent to Belfort; scenarios Breakaway 64 / Reduced sprint 16 / GC attack 12 / Full sprint 8; favourites Mohorič/van der Poel/Pidcock/Healy/Pedersen, dark horse Alaphilippe). Bumped `defaultStageId` to 2026-13 and moved the four default-stage test pins — this morning every not-yet-briefed sentinel moved 2026-13→2026-14 (the `loadAllStagePackets` null-example + the "no packet derives to future" derive test in `briefing-data.test.ts`; the injected-load-failure test in `briefing.test.tsx`), plus DEFAULT_STAGE_HEADING ("Stage 13: Dole to Belfort"), the `205.8 km` basics assertion, and the round-trip default chip (`/Stage 13,/i`). Gates green (737 Vitest, lint 0, build clean); committed `b91fb90`, pushed to origin, deployed via `npx vercel --prod` (dpl_4BjPkSo7svwL1zt74bXsDZY3wAgT), live-verified on prod (Briefed pre-stage badge, 205.8 km, as-of 2026-07-17). Caveats: the run landed ~14:08 CEST, after the 205.8 km stage's likely midday roll-out, so R20 (publish before the start) was MISSED — kept the conventional 07:45 morning `asOf` (content is a genuine pre-stage read) and flagged it to the user; per section 9 (no same-day republish) it stands. Judgment calls: elevationGainM 2500 from the tourdefranceparcours ASO road-book mirror over the 2400 preview cluster (letour.fr still WebFetch-blocked); weatherSnapshot from a real Open-Meteo query for Belfort (~27 C, ~48% shower chance, gusts ~44 km/h, supports the wet-descent read) and cited; image step skipped (optional). No web-app-resume code changes. Lessons in auto-memory (`cycling-application-site.md`). Next morning: grade stage 13, publish stage 14 (Mulhouse to Le Markstein Fellering, mountain).
- **Sibling project (2026-07-16, daily briefing)** — Ran the cycling site's daily briefing ritual for stage 12 and deployed, per `~/projects/cycling-application/docs/briefing-ritual.md`. Graded stage 11 (Vichy to Nevers): Søren Wærenskjold (Uno-X Mobility) won the full bunch sprint after the four-man break was caught late, so `outcome` records `scenarioOccurred: "Full sprint"` (a correct top-scenario call) and `topFavoriteWon: false` (winner not on the packet's favourites list); added as a clean +10-line edit, no reformat. Researched + published the stage 12 live packet (Circuit Nevers Magny-Cours to Chalon-sur-Saône, 179.1 km flat, three cat-4 climbs with the last ~20 km out, penultimate sprint chance; favourites Kooij/Merlier/Wærenskjold/Philipsen/Girmay, Full sprint 74%, dark horse Max Kanter). Bumped `defaultStageId` to 2026-12 and moved the default-stage test pins — note there were FOUR pins, not the two the runbook documents: `briefing-data.test.ts` line 124 (default id) + the `loadAllStagePackets` "null example" id (had to move OFF 2026-12 once it became the cached default) + its "no packet" derive test (cache-collision: the first suite caches the default id, so the second must use a different stage), and `briefing.test.tsx` DEFAULT_STAGE_HEADING + the round-trip default chip + the `161.3 km` basics assertion + the injected-failure test (moved to a NON-default briefed stage so the default's mount-load can't consume its one-shot rejection). Gates green (736 Vitest, lint 0, build clean); committed `9ac1f7e`, pushed to origin, deployed via `npx vercel --prod` (dpl_Gtiq6MN7uLRnzSd2nEz6aiz4exPA), live-verified on prod ~6 min before the 13:30 stage start (R20 met). Judgment calls: elevationGainM 1800 from preview consensus (official letour.fr was unreachable via WebFetch, PJAMM's 1542 was the GPS outlier); weatherSnapshot from a real Open-Meteo forecast query (~35 C, hotter than the previews' "heatwave easing ~30 C" framing) and cited; image step skipped (optional). No web-app-resume changes. Lessons in auto-memory (`cycling-application-site.md`). Next morning: grade stage 12, publish stage 13 (Dole to Belfort, hilly).
- **Sibling project (2026-07-15, deploy)** — Executed the cycling site's operator tail on user request: stage 11 first LIVE morning publish (Vichy to Nevers, before stage start), all 9 remaining raced stages backfilled as reconstructed packets (archive = 11 of 21 briefed, 734 tests green), and the site DEPLOYED: private GitHub repo Ninety2UA/cycling-application, Vercel project (GEMINI_API_KEY + NEXT_PUBLIC_TEAM=bahrain), live at https://bahrain-victorious.dbenger.com with noindex (user's decisions via blocking question: deploy now, noindex until send, that domain, private repo). Live-verified end to end incl. the prod concierge. Vercel-GitHub auto-deploy connect needs a dashboard grant; until then morning publishes end with `npx vercel --prod`. Pre-SEND items stay with Dominik: copy approval, team-fact refresh, quota cap, analytics toggle, link previews, send to Eržen. Daily ritual continues through 2026-07-26. Post-deploy same session: Briefing renamed "TdF 2026 Briefing" and moved first in nav (md-only abbreviation) + first flagship card on home; hook card reworked to a 7/5 two-column spread via a new BookendCard `aside` slot (screenshots verified 390-1440px); full 14-row concierge QA re-run on prod (all PASS after one KB fix: the assistant claimed 2026 results "have not yet occurred" mid-Tour); full browser-smoke checklist run on prod (all sections green; checklist's stale 12-prototype list corrected to 14; Vercel preview deploys can't do the keyless drill — deployment protection 401s first).
- **Sibling project (2026-07-14/15, implementation)** — BUILT the TdF 2026 Stage Briefing third flagship in `~/projects/cycling-application` via `/ce-work`, executing the 2026-07-14 plan to its Definition of Done: `main` ff-merged `246eb0f..8962b80` (16 commits, 91 files), all gates green (722 Vitest, lint 0, build clean, 162 pytest, deterministic geometry regen, dev-server smoke + live concierge QA logged). U1-U10 built by parallel subagents on a pinned stub contract (zero merge conflicts); simplify pass consolidated cross-unit duplication; multi-agent review (9 personas + 11 validators, codex cross-model pass skipped: local sandbox host binary missing) confirmed 10 findings, all fixed pre-merge (P0 publish-gate shape gaps, two mid-Tour test time bombs keyed to not-yet-committed packets, fmtDateShort UTC off-by-one, weather archive-label leak, RouteMap console import); residuals recorded in that repo's `docs/residual-review-findings/feat-tdf-stage-briefing.md`. Landing default is the reconstructed stage 6 exemplar (Pau to Gavarnie-Gedre, genuinely sourced) until the first live morning publish re-bumps it. Repo still local-only, NOT deployed. Operator tail: backfill raced stages as reconstructed packets, first live publish (stage 11, 2026-07-15) before stage start per `docs/briefing-ritual.md`, deploy per that repo's pre-send checklist. No web-app-resume changes beyond this note; lessons in auto-memory (`cycling-application-site.md`).
- **Sibling project (2026-07-14, planning only)** — Planned the THIRD flagship for `~/projects/cycling-application`: **TdF 2026 Stage Briefing** (`/briefing`, nav "Briefing"). Full ce-brainstorm → ce-plan → ce-doc-review pipeline produced one implementation-ready unified plan at `cycling-application/docs/plans/2026-07-14-001-feat-tdf-stage-briefing-plan.md` (committed in that repo): per-stage seven-section pre-stage briefing (user's V5 spec) plus GPT Image prompt + committed 3:4 PNG below; daily morning research ritual commits per-stage JSON (official letour.fr sources first, model-probability framing, zero betting vocabulary — "value pick" renamed dark-horse pick); live Open-Meteo weather reuse; full 21-stage archive with labeled reconstructed backfill; self-scoring calibration (counts-first, reconstructed/neutralized excluded); three-flagship copy rewrite (six strings + PRODUCT.md); deploy-with-tool (Tour ends 2026-07-26 — every build day converts a live-briefing day into backfill). 5-persona doc review applied 12 fixes into the plan incl. a confidence-100 loader-mechanism bug. Implementation handed to a NEW session via a `/goal` prompt (start that session in the cycling repo). No web-app-resume changes; verified repo facts + lessons in auto-memory (`cycling-application-site.md`).
- **Sibling project (2026-07-13/14)** — Big cycling-application session (`~/projects/cycling-application`, 24 commits `3c9dec0..18c3927`, tree clean, still NOT deployed): executed the KLAR-native restructure plan U1–U10 to its Definition of Done (page grammar: hook/ask bookends, verdict-first expandable /analysis, /about proof matrix, site-wide resume download, zero-em-dash copy pass incl. pipeline-generated JSON with a durable copy-gates test), ran the multi-agent code review + fixed all 7 findings + recorded residuals, extracted the official Bahrain Victorious wordmark + teal from bahraincyclingteam.com (shipped #007e88, their #00adbb fails AA), then two user-driven premium polish rounds (centred hero with accent payoff line, hook tint shrunk to a card, ranked single-accent atlas charts, centred ask cards, fell-in-love cycling copy, warmer about hero). Review gallery artifact: https://claude.ai/code/artifact/1f3d1406-7ffc-42ca-a609-0f350e712144 . No web-app-resume changes; that repo's AGENTS.md/DESIGN.md are the source of truth; lessons in auto-memory (`cycling-application-site.md`).
- **Sibling project (2026-07-03)** — Built the Amazon AIP interactive job application at `~/projects/amazon-application` (own repo, committed at `7de0eb5`; deploy to `amazon.dbenger.com` pending user go-ahead). No web-app-resume changes that session. That project's own AGENTS.md/docs are the source of truth; cross-project pitfalls recorded in auto-memory (`amazon-aip-application-site.md`).
- **Latest work (2026-07-10)** — Repo housekeeping: committed Phase 29 doc sync (`43ec3df`), cycling WorldTour outreach planning docs (`25fc0f3`), and AGENTS.md + `.gitignore` entries for agent scratch (`.agents/`, `.playwright-cli/`, `chatbot/`, `skills-lock.json`) (`33e66f9`). Deleted merged branches `feat/projects-page` + `feat/resume-v4-update` (local) and `origin/feat/projects-page` (remote). Tree clean, only `main` remains, no worktrees. All three commits doc-only — live site unchanged. `AGENTS.md` mirrors `CLAUDE.md` for Codex-style agents (two wording tweaks); keep in sync when CLAUDE.md changes materially.
- **Prior work** — Scroll performance polish (commit `2d553ad`). Diagnosed "slow-motion / sluggish" scroll feel via Chrome DevTools traces: no frame jank (120 fps flat, 0 long frames at 120 Hz), perception driven by 0.6s ease-out-quint transitions + 0.6s max delay (1.2s end-to-end fade) across 118 `[data-animate]` elements. Applied seven coordinated fixes mirrored in `public/site.html` + `docs/index.html`: (1) `[data-animate]` duration 0.6s→0.35s (+ 0.5s variant→0.3s); (2) delay step 0.1s→0.04s (max stagger 0.6s→0.24s); (3) easing `cubic-bezier(0.16,1,0.3,1)`→`cubic-bezier(0.22,1,0.36,1)` (ease-out-quart); (4) removed `mix-blend-multiply` from 4 decorative `blur-3xl` circles (GPU can composite instead of CPU paint); (5) removed `animate-pulse` from 2 of those circles (no more continuous repaint); (6) nav `bg-white/80 backdrop-blur-md`→`bg-white/95` (no per-scroll-frame backdrop-filter); (7) `.page-view > section:nth-of-type(n+2) { content-visibility: auto; contain-intrinsic-size: auto 1200px }` for below-fold render deferral. End-to-end fade per element: 1.2s → 0.59s.
- **Earlier work** — Phase 28 hero canvas rework (`396c47a`): all 27 skill words render via Poisson-disk layout (desktop) / 2-col stagger (mobile), Lissajous oscillation, proximity highlight, ambient constellation lines, DPR-scaled, IO-paused. FEATURE 5 block must stay byte-identical between `public/site.html` and `docs/index.html`.
- **Deployed** — `2d553ad` live at https://dbenger.com (Vercel dpl_948pZ85Yz6GRPVkMHhZhks34gYDA, 37s build+deploy). Manual deploy via `npx vercel --prod` when Git auto-deploy doesn't trigger.
- **Perf diagnostic playbook** — To distinguish jank from perception: (a) run scripted rAF scroll via Chrome DevTools MCP `evaluate_script` and measure frame deltas; (b) inject CSS overrides (kill `backdrop-filter`, `filter: blur()`, `animate-pulse`, `[data-animate]` transitions) and re-measure; (c) if frame timing identical, the fix is perceptual (shorten/snappier animations), not compositing.
- **Mobile nav** — [logo] Experience Projects Collab Contact [PDF Resume]. "Home" hidden on mobile (`hidden sm:inline`). "Collaboration" abbreviated to "Collab" on mobile to fit 5 links. Ebook accessible via hero pill. Text `11px` and padding `px-3` at base for small Android screens.
- **Desktop nav** — [logo + "How I Built This Web App" pill] Home Experience Projects Collaboration Contact [PDF Resume]. No "Ebook" text link — pill replaces it.
- **Projects page** — 6 cards with type badges (Claude Code Plugin/violet, SaaS Product/emerald, Full-Stack App/sky, Automation Pipeline/amber). "Explore More Projects" CTA links to projects.dbenger.com. Requirements doc at `docs/brainstorms/projects-page-requirements.md`.
- **Resume PDF** — V4 at `~/Documents/Resume & Jobs/Dominik Benger - Resume [V4].pdf`. User edits source doc and re-exports; copy to `public/resume/Dominik_Benger_Resume.pdf` when updated. PyMuPDF PDF text editing produces font mismatches (Helvetica vs Carlito) — don't attempt.
- **Hero ebook pill** — Mobile-only (`sm:hidden`) teal pill above status badge, links to `#ebook`.
- **Agent scratch dirs** — `chatbot/`, `.agents/`, `.playwright-cli/`, `skills-lock.json` are gitignored (since `33e66f9`); never commit them.
